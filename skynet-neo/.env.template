# Copy this file to .env (same directory) and fill in your values.
# .env is gitignored so your credentials never get committed.
# NOTE: Vite bakes these into the JS bundle at build time - rebuild after changing.
VITE_NEO_APP_USERNAME="neo4j"
VITE_NEO_APP_PASSWORD="YOUR_NEO4J_PASSWORD"
VITE_NEO_APP_DB_URL="http://localhost:7474/db/neo4j"
